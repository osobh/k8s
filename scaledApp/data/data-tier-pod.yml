apiVersion: v1
kind: Pod
metadata:
    name: data-pod
    labels:
      app: ticker
      tier: data
spec:
 containers:
   - name: redis
     image: "redis:latest"
     imagePullPolicy: IfNotPresent
     ports:
       - containerPort: 6379
         protocol: TCP
